// Package components is a compatibility anchor for Paper's component packages.
//
// Components live in focused subpackages such as text, image, code, row, col,
// table, and richtext. Import those packages directly; this package remains
// importable in v2 for users that already reference the aggregate path.
package components
